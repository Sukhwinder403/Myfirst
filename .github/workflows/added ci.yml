name: workflow demo
on:
  push:
    branches: [ "main" ]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Build and push Docker images
  uses: docker/build-push-action@v6.16.0

